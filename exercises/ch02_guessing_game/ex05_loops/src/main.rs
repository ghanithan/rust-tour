use std::io;
use std::cmp::Ordering;
use rand::Rng;

fn main() {
    println!("I'm thinking of a number between 1 and 10!");
    
    // TODO: Generate a random target number between 1 and 10 (inclusive)
    // Hint: Use rand::thread_rng().gen_range(1..=10)
    
    // TODO: Create a variable to track the number of attempts
    // Hint: let mut attempt_count = 0;
    
    // TODO: Create an infinite loop using the 'loop' keyword
    // Inside the loop, you need to:
    
    // TODO: Increment the attempt counter at the start of each iteration
    
    // TODO: Prompt the user for their guess (show attempt number)
    
    // TODO: Read the user's input
    
    // TODO: Parse the input to a number, handling parsing errors
    // If parsing fails, show an error message and use 'continue' to try again
    // If the number is outside 1-10 range, show error and continue
    
    // TODO: Compare the guess to the target number using match
    // - If guess is less than target: print "Too low! Try again."
    // - If guess is greater than target: print "Too high! Try again." 
    // - If guess equals target: print success message with attempt count and 'break' the loop
    
    // Your implementation here:
    
}